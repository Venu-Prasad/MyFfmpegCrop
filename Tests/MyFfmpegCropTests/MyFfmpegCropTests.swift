import XCTest
@testable import MyFfmpegCrop

final class MyFfmpegCropTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyFfmpegCrop().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
